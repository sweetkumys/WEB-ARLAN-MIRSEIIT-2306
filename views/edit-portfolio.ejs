<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/styles.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Portfolio Item</title>
</head>
<body>
  <h1>Edit Portfolio Item</h1>
  <form action="/portfolio/<%= portfolio._id %>" method="POST">
    <label for="title">Title:</label>
    <input type="text" id="title" name="title" value="<%= portfolio.title %>" required>
    <br>
    <label for="description">Description:</label>
    <textarea id="description" name="description" required><%= portfolio.description %></textarea>
    <br>
    <label for="images">Images (comma separated URLs):</label>
    <input type="text" id="images" name="images" value="<%= portfolio.images.join(',') %>" required>
    <br>
    <button type="submit">Update</button>
  </form>
</body>
</html>